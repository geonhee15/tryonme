# TRYONYOU

프롬프트 말고. 사진 한 장 올리고, 진짜 제품을 골라서 그대로 입어보는 가상 피팅.

다른 AI 트라이온 사이트들이 "이 사람한테 검은 셔츠 입혀줘" 같은 텍스트 프롬프트로 옷을 생성하는 것과 달리, TRYONYOU는 실제 브랜드의 실제 제품을 카탈로그에서 골라 그대로 입혀줍니다. 여러 벌 동시 선택도 가능하고, 카탈로그에 없는 옷은 프롬프트로도 추가할 수 있어요.

## 어떻게 쓰나요

1. 전신 사진을 올리세요 (드래그 또는 클릭).
2. 아래 카탈로그에서 원하는 상의를 골라요. 여러 개 선택 가능.
3. 카탈로그에 없으면 직접 설명도 OK.
4. **입혀보기** 버튼을 누르면 합성된 미리보기가 나옵니다.

## 지금 단계

- ✅ 상의 (티셔츠, 후디, 셔츠, 자켓, 코트, 니트 등 30+)
- ⏳ 하의 — 다음
- ⏳ 신발 — 다음
- ⏳ 안경 — 다음

## 로컬에서 띄우기

```bash
python3 -m http.server 5188
# → http://localhost:5188
```

`index.html` 하나로 동작합니다. 빌드 단계 없음. React + Babel CDN.

## 배포 예정

`tryonyou.kimkim.io` 서브도메인에 정적 호스팅으로 올라갑니다.

## 현재 try-on 엔진

지금은 **데모 모드** — 업로드한 사진에 선택한 제품 썸네일을 캔버스로 합성해 보여줍니다. 실제 가상 피팅 AI(예: fal.ai의 idm-vton)는 `runTryOn()` 함수를 교체해 연결하면 됩니다 — 함수 시그니처는 `{ userPhotoDataUrl, selectedItems, customPrompt }` → `Promise<dataUrl>` 입니다.

## 참고

- UI 스타일: [대신콜](https://daesincall.jocoding.io/)의 디자인 언어 (따뜻한 크림 배경 + 코럴 액센트 + Pretendard).
